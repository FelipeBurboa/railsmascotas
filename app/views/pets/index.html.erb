<p id="notice"><%= notice %></p>

<h1>Mascotas</h1>

<table class="table">
  <thead class="table-dark">
    <tr>
      <th>Nombre</th>
      <th>Raza</th>
      <th>Fecha nacimiento</th>
      <th>Visitas</th>
      <th>Ficha Mascota</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pets.each do |pet| %>
      <tr>
        <td><%= pet.name %></td>
        <td><%= pet.race %></td>
        <td><%= pet.birthdate %></td>
        <td><%= pet.history_count%></td>
        <td><% pet.pet_histories.each do |history| %>
                    Descripción: <%= history.description %>, Fecha: <%=history.created_at.year%>/<%=history.created_at.month%>/<%=history.created_at.day%> <br />
                        <% end %></td>
        <td><%= link_to 'Show', pet %></td>
        <td><%= link_to 'Edit', edit_pet_path(pet) %></td>
        <td><%= link_to 'Destroy', pet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to 'New Pet', new_pet_path %>
